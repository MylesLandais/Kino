import Config

# Production config is handled in runtime.exs via environment variables.
# This file exists so `import_config "#{config_env()}.exs"` does not fail
# when MIX_ENV=prod (required for `mix release` / Nix builds).
